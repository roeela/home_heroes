# HomeHeroes — ניהול משימות ביתיות

Flutter + Firebase Android app for a single family to manage home chores.
All UI is in **Hebrew with RTL layout** (Rubik font).

---

## Quick start

### Prerequisites
- Flutter SDK installed
- Android SDK installed
- A Firebase project (see Firebase Setup below)

### 1. Install dependencies
```
flutter pub get
```

### 2. Configure Firebase (one-time)
See **Firebase Setup** section below.

### 3. Run
```
flutter run
```

---

## Firebase Setup

> Until this is done the app crashes on startup with an `UnsupportedError` from `firebase_options.dart`.

### Step-by-step

1. **Create a Firebase project** at https://console.firebase.google.com

2. **Enable services** in the Firebase Console:
   - Authentication → Sign-in method → Google (enable it)
   - Firestore Database → Create database (start in **test mode** for development)

3. **Install FlutterFire CLI** (once per machine):
   ```
   dart pub global activate flutterfire_cli
   ```

4. **Configure** from inside the `home_heroes` directory:
   ```
   flutterfire configure
   ```
   - Select your Firebase project
   - Choose Android only
   - This overwrites `lib/firebase_options.dart` and `android/app/google-services.json`

5. **Add your debug SHA-1** to Firebase Console → Project Settings → Your Android App → Add fingerprint:
   ```
   keytool -list -v -keystore %USERPROFILE%\.android\debug.keystore -alias androiddebugkey -storepass android -keypass android
   ```
   Copy the `SHA1:` line and paste it into Firebase Console.
   Then **re-download `google-services.json`** from Firebase Console → Project Settings → Your Android App and replace `android/app/google-services.json`.

6. **Deploy Firestore indexes** (requires Firebase CLI):
   ```
   firebase login
   firebase use <your-project-id>
   firebase deploy --only firestore:indexes
   ```
   Alternatively, the app will prompt you with a direct link to create each index the first time a query that needs it runs.

7. Run `flutter run` — the app should launch and Google Sign-In should work.

### Firestore security rules (after dev)

Replace the default permissive rules in Firebase Console → Firestore → Rules:
```
rules_version = '2';
service cloud.firestore {
  match /databases/{db}/documents {
    match /family/{familyId}/{document=**} {
      allow read, write: if request.auth != null
        && exists(/databases/$(db)/documents/family/$(familyId)/users/$(request.auth.token.email));
    }
  }
}
```

---

## First-time app flow

1. **Primary parent** opens app → signs in with Google
2. No family found → taps "צור משפחה חדשה" → enters family name → family is created
3. Parent goes to **Family tab** → "+" → enters each child's Gmail + display name + weekly quota
4. Each child opens app → signs in with Google → automatically joins the family

---

## How tasks work

Tasks come in three types:

| Type | Hebrew | Description |
|---|---|---|
| **Daily** | יומי | Tied to specific days of the week (e.g. Feed dog every Sun/Wed). Children see these in "משימות היום" only on the scheduled day. |
| **Weekly** | שבועי | Flexible — any child can claim them any time before the week ends. Appear in "זמין השבוע". |
| **Bonus** | בונוס | Optional one-off tasks a parent adds during the week. Appear in "זמין השבוע" with a star icon. |

### Points & rewards
- Children earn points when a parent approves a completed task.
- Points count toward a **weekly quota** set per child.
- Excess points beyond the quota are tracked as **available reward points** ("נקודות לפרס").
- The parent can tap **"תן פרס"** on a child's card (Overview tab) to give a real-life reward and zero out the excess.
- Unspent excess carries forward to the next week; debt (earning below quota) also carries forward.

### Week
- The Israel week runs **Sunday → Saturday**.
- Week initialization is idempotent and runs automatically when either parent or child opens the app.

---

## Tech stack

| Layer | Technology |
|---|---|
| UI framework | Flutter (Android target) |
| State management | Riverpod 2 (`flutter_riverpod`) |
| Navigation | GoRouter 14 |
| Backend | Firebase Auth + Cloud Firestore |
| Authentication | Google Sign-In |
| Font | Rubik (Google Fonts — Hebrew support) |
| Locale | Hebrew (`he`) — full RTL |

---

## Project structure

```
lib/
├── main.dart                        # App entry point, Firebase init, MaterialApp.router
├── firebase_options.dart            # Generated by `flutterfire configure`
│
├── core/
│   ├── theme/app_theme.dart         # Material 3 theme, Rubik font, terracotta/sand/sage palette
│   ├── routing/app_router.dart      # GoRouter with auth-based redirect and role guard
│   └── utils/week_utils.dart        # getWeekStart() [Sunday], weekLabel(), weekStartId()
│
└── features/
    ├── auth/
    │   ├── providers/auth_provider.dart    # Firebase/Riverpod providers + signIn/signOut/createFamily
    │   └── screens/
    │       ├── login_screen.dart           # Google Sign-In (HomeHeroes logo)
    │       ├── family_setup_screen.dart    # New family creation (first parent only)
    │       └── no_family_screen.dart       # Error: email not in any family
    │
    ├── shared/
    │   ├── models/                         # family, family_user, chore, chore_instance, weekly_balance
    │   ├── repositories/                   # family, user, chore, instance, balance repositories
    │   └── widgets/
    │       ├── app_loading.dart            # Centered CircularProgressIndicator
    │       └── score_chip.dart             # Styled score badge (e.g. "10 נק׳")
    │
    ├── parent/
    │   ├── providers/parent_providers.dart # Repository providers + stream providers + approveChore()
    │   └── screens/
    │       ├── parent_dashboard.dart       # 4-tab dashboard (Overview / Chores / Approvals / Family)
    │       ├── chore_form_screen.dart      # Add / edit chore (type selector + day picker for daily)
    │       └── add_member_screen.dart      # Pre-add a family member by email
    │
    └── child/
        ├── providers/child_providers.dart  # currentWeekStart, openInstances, myInstances, myBalance
        └── screens/
            └── child_dashboard.dart        # 2-tab dashboard (הבית / המשימות שלי)
```

### Parent dashboard tabs

| Tab | Label | Contents |
|---|---|---|
| 0 | סקירה | Per-child cards showing progress, excess reward points, and "תן פרס" button |
| 1 | משימות | List of all chores; tap to edit, long-press to delete, FAB to create |
| 2 | אישורים | Completed tasks awaiting approval — approve or reject |
| 3 | משפחה | Family members list; primary parent can add/remove members |

### Child dashboard tabs

| Tab | Label | Contents |
|---|---|---|
| 0 | הבית | Weekly goal card → "משימות היום" (today's daily tasks) → "זמין השבוע" (weekly + bonus tasks) |
| 1 | המשימות שלי | Claimed tasks grouped by status: בביצוע / ממתין לאישור / אושר |

See [ARCHITECTURE.md](ARCHITECTURE.md) for Firestore schema, user flows, and design decisions.
